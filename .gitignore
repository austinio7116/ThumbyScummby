# Reference source — not our work
scummvm-upstream/

# Proprietary game data (user must provide their own)
data/

# Build artifacts
build/
build_device/
device_pico/build_mi1/
device_pico/build_mi2/
device_pico/build_indy3/
device_pico/build_indy4/
# Legacy build dir names (kept for old worktrees)
build_mi1/
build_atlantis/
device_pico/build_atlantis/
device_pico/build/
*.o
*.a
*.so
*.uf2
*.elf
*.dwo
*.dwp

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
compile_commands.json

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Test artifacts
*.log
*.ppm
*.png
# Picker thumbnails are part of the firmware build (baked in via
# pack_scumm_thumbs.py), keep them tracked.
!device_pico/thumbs/*.png

# OS
.DS_Store
scummvm.ini
scummvm
*.ini
.claude/

# Runtime config (host-side persistent settings)
thumbyscummby.cfg

# Host-side save file (mirrors the device flash slot, one per data dir)
thumbyscummby.sav
*.sav

# SCUMM script disassemblies (descumm output)
/disasm/
/dumps/
